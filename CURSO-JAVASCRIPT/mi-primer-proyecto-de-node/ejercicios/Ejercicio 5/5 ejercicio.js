const altura_cm = 178
const altura_m = 1.78
const peso_kg = 77.8

const altura_redondeada = Math.ceil(altura_m)
const peso_redondeado = Math.floor(peso_kg)

const sonIgual = Number.MAX_VALUE + 1 === Number.MAX_VALUE